export default class TabClosed {
  constructor({id, amountPaid, orderValue, tipValue }) {
    this.id = id
    this.amountPaid = amountPaid
    this.orderValue = orderValue
    this.tipValue = tipValue
  }
}
